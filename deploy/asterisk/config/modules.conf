[modules]
autoload=yes
preload=res_chan_isdn.so
preload=res_convert.so
preload=res_format_wav.so
preload=res_format_pcm.so

[modules]
; Load any required modules here
; For basic operation, we need these modules
load = res_pjsip.so
load = res_pjsip_pubsub.so
load = res_pjsip_exten_state.so
load = res_pjsip_sdp_rtp.so
load = res_pjsip_transport_websocket.so
load = res_rtp_asterisk.so
load = app_dial.so
load = app_verbose.so
load = bridge_simple.so
load = chan_pjsip.so

; Additional modules for HTTP/ARI support
load = res_http_websocket.so
load = res_ari.so
load = res_ari_applications.so
load = res_ari_channels.so
load = res_ari_bridges.so

; Basic dialplan functionality
load = pbx_config.so