; HTTP Configuration for Asterisk REST Interface (ARI)
; This enables the ARI that our backend uses to control Asterisk

[general]
enabled=yes
bindaddr=0.0.0.0
bindport=8088
tlsenable=no
tlsbindaddr=0.0.0.0:8088
;tlscertfile=/etc/asterisk/keys/asterisk.crt
;tlsprivatekey=/etc/asterisk/keys/asterisk.key
; NOTE: Certificate files will be generated during deployment
enablestatic=yes
; No prefix for ARI
; CORS settings for development
allowed_origins=*
allowmultiple=true
; Session limit for ARI
sessionlimit=100
; WebSocket support
websocket_enabled=true
bindport_ws=8088