; RTP Configuration for media streams
; This defines the ports and settings for RTP media

[general]
rtpstart=10000
rtpend=10100
ice_support=yes
stunaddr=stun.l.google.com:19302
; These ports must match the Docker compose port mapping